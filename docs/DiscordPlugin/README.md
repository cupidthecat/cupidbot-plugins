# Discord Notifier

Sends selected local plugin notifications to a configured Discord webhook.

## Setup

1. Configure a Discord webhook URL in the RuneLite profile settings.
2. Enable Discord Notifier.
3. Select which events should send notifications.

Networking is plugin-owned and only runs when this local plugin is enabled and configured.
